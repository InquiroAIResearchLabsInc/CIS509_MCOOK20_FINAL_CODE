# Lean install used by Codespaces bootstrap and the --skip-llm --skip-topics
# fast path the professor runs first. No torch, no transformers, no umap/hdbscan.
# Full ML stack is in requirements.txt (kept for reproducibility).

pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
matplotlib==3.9.2
xgboost==2.1.2
vaderSentiment==3.3.2
groq==0.13.0
python-dotenv==1.0.1
blake3==0.4.1

# Notebook runtime, no Jupyter metapackage (which pulls in qtconsole etc.)
notebook==7.2.2
nbconvert==7.16.4
nbformat==5.10.4

# MCP server
mcp>=1.10,<2.0

# Tests
pytest==8.3.3
pytest-cov==5.0.0
