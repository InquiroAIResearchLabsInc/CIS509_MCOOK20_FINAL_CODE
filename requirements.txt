# Full TABHS dependency set (lean base + heavy ML stack for BERTopic).
# For the fast Codespaces bootstrap, install requirements-base.txt instead.
# Strategy doc lines 396-412, plus MCP and test additions.

# Lean base, kept identical to requirements-base.txt
-r requirements-base.txt

# Heavy ML stack used by BERTopic. PyTorch is installed CPU-only via the
# devcontainer install command (--extra-index-url for download.pytorch.org/whl/cpu),
# which saves ~1.5 GB of unused CUDA wheels.
bertopic==0.16.4
umap-learn==0.5.6
hdbscan==0.8.40
sentence-transformers==3.2.1
